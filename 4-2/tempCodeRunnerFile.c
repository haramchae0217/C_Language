
    // double c = 1.0;
    // int *pa, *pb;
    // double *pc = &c;
    // pa = &a;
    // pb = &b;
    // *pa = *pa + *pb;
    // *pb = *pa + *pb;
    // *pc = *pc / 2.0;
    // double result = (a+b+c) / sizeof(pc);
    // // a = 2, b = 3, c = 0.5
    // // sizeof(pc) = 8
    // printf("%.6f",result);