#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <stdlib.h>

int main() {
  // 실습 -1
  // int *pi;
  // double *pd;

  // pi = malloc(sizeof(int));
  // if(pi == NULL) {
  //   printf("# 메모리가 부족합니다.\n");
  //   exit(1);
  // }

  // pd = malloc(sizeof(double));

  // *pi = 10;
  // *pd = 3.4;

  // printf("정수형으로 사용 : %d\n", *pi);
  // printf("실수형으로 사용 : %.1f\n", *pd);

  // free(pi);
  // free(pd);


  return 0;
}